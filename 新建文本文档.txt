web-xml:配置监听器加载配置文件

###### 				过滤器

filtername:springSecurityfilterChain

filterClass:delegatingFilterProxy

contextLoaderListener



security.xml:页面拦截规则，认证管理器



authentication认证

BCrypt算法对密码进行加密。先加入特定字符串加密，然后在加入随机salt，

富文本：引入js，获取文本内容editor.html();

multipartResolver,多媒体解析器

jsp jQuery ajax sql优化，

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

native就是调用非java程序的接口

this关键字主要是为了解决，局部变量影藏成员变量的问题，因为jvm的就近原则

final方法不能被重写

值传递和引用传递区别:因为无论是按值传递还是按引用传递都是把值传递过去了，所以就叫按值传递。

JDK1.8之后有哪些新特性，在项目中哪里用到了

1.修饰接口的abstact,也可以用default

2.lamda表达式

3.optional

**Comparable可以认为是一个内比较器。**可以和相同类型的对象作比较，也可以和不同类型的对象做比较

**Comparator可以认为是一个外比较器**。有两种情况下会使用Comparator。

1. 对象没有实现Comparable接口。但又想对两个对象进行比较。

2. 对象实现了Comparable接口，但是comparaTo方法中的比较条件不满足需求。

   **Object类和范型的区别,说出范型的执机制，为什么要使用范型**

没有泛型的情况的下，通过对类型Object的引用来实现参数的“任意化”，“任意化”带来的缺点是要做显式的强制类型转换，而这种转换是要求开发者对实际参数类型可以预知的情况下进行的。对于强制类型转换错误的情况，编译器可能不提示错误，在运行的时候才出现异常，这是一个安全隐患。泛型的好处是在编译的时候检查类型安全，并且所有的强制转换都是自动和隐式的，提高代码的重用率





cp -r 复制文件

ifconfig 查看ip

tar -zxvf 压缩文件名

ps -le查看进程

：x退出

:wq保存

vim

用editplus++ ftp设置-sftp连接

遇到什么问题：

交换机持久化，队列持久化，消息持久化